# 使い方（USAGE）

Bookmark Manager PE は、**ブラウザのブックマークをひとつのアプリに集約して管理する**ためのツールです。

- 複数ブラウザ（Chrome / Firefox / Safari / Edge）のブックマークをまとめてインポート
- タグ付け・検索・重複整理でコレクションを整頓
- AI（Cloudflare Kitesurf / Zed AI）で自動要約・自動タグ付け
- GitHub リポジトリ経由で複数デバイス間の同期

---

## 1. インストールと起動

### 必要なもの

- **Node.js** v20 以上
- **pnpm** v9 以上（`npm install -g pnpm` で導入）

### セットアップ

```bash
git clone https://github.com/watanabe3tipapa/bookmark-manager-pe.git
cd bookmark-manager-pe
pnpm install
```

`pnpm install` の最後に、SQLite 用のネイティブモジュールが Electron 向けに自動ビルドされます。

### 起動

```bash
pnpm dev
```

初回は数秒かかりますが、アプリのウィンドウが開きます。

---

## 2. 画面の見方

画面は **3 つのパネル** で構成されています。

| パネル | 場所 | 役割 |
|---|---|---|
| サイドバー | 左 | 検索・スマートビュー・タグ・デバイススコープ・同期 |
| ブックマーク一覧 | 中央 | 絞り込んだブックマークの一覧と操作ボタン |
| 詳細パネル | 右 | 1件のブックマークの編集・タグ操作 |

中央の一覧でブックマークをクリックすると右に詳細パネルが開き、タイトル・URL・メモの編集やタグの追加・削除ができます。

---

## 3. 基本操作

### ブックマークを追加する

1. 一覧上部の **追加** ボタンをクリック
2. URL を入力（タイトル・タグは省略可）
3. Enter で保存

### ブラウザのブックマークをインポートする

各ブラウザで **HTML ファイルを書き出して**、アプリの **インポート** ボタンから取り込みます。

- **Chrome**: ブックマークマネージャ（`Cmd + Option + B`）→ `⋯` → ブックマークをエクスポート
- **Firefox**: ライブラリ（`Cmd + Shift + O`）→ インポートとバックアップ → HTML 形式でエクスポート
- **Safari**: ファイル → ブックマークを書き出す
- **Edge**: お気に入りの管理 → `⋯` → お気に入りをエクスポート

> ポイント
> - フォルダ構造は自動的に **タグ** として取り込まれます
> - 複数のブラウザから順番にインポートして、全部のブックマークを1つの場所に集められます
> - インポートしてもブラウザ側のデータは一切変更されません

### 削除・タグ編集

- 詳細パネルからタイトル・URL・メモの編集、タグの追加・削除、削除ができます
- 一覧の行に表示されたタグの `×` を押すと、そのタグだけ外せます

---

## 4. 検索

サイドバー上部の検索ボックスに文字を入力すると、**タイトル・URL・タグ** を横断検索します。検索中もスマートビューやタグフィルタはそのまま適用されます。

---

## 5. 整理機能

### スマートビュー（サイドバー上部）

状況に合わせて一覧を切り替えられます。

| ビュー | 内容 |
|---|---|
| すべて | 全ブックマーク（デフォルト） |
| 未分類 | タグが1つも付いていないもの |
| 最近追加 | 7日以内に追加したもの |
| 使われていない | 追加から30日以上経過し、一度も開いていないもの |
| よく見る | 訪問回数が多いもの |

### タグフィルタ

サイドバーのタグをクリックすると、そのタグの付いたブックマークだけに絞り込めます。

### デバイススコープ（サイドバー下部）

| スコープ | 内容 |
|---|---|
| すべて | 全ブックマーク |
| 共有のみ | 複数デバイスで共有するもの |
| デバイス固有のみ | このデバイスだけのもの |

詳細パネルで各ブックマークを「共有 / デバイス固有」に切り替えられます。

### 重複検出・マージ

同じページを複数回保存してしまった場合の整理機能です。

1. 一覧上部の **重複** ボタンをクリック
2. 検出された重複グループを確認
3. グループ単位で **マージ**（統合）またはスキップを選択

URL は自動的に正規化（`utm_*` 等の広告パラメータを除去）して比較するので、実質同じページは確実に重複判定されます。

マージ時の統合ルール:

| 項目 | ルール |
|---|---|
| タイトル | 一番長いタイトルを採用 |
| タグ | 両方のタグを合体 |
| 訪問回数 | 合算 |
| メモ | 改行で連結 |

---

## 6. 探索機能（AI 要約・サムネイル・リンク発見）

Cloudflare のブラウザ（Kitesurf）を使って、保存したブックマークを**自動で分析**します。

- 日本語で 2〜3 文の **AI 要約** を作成
- ページの **サムネイル画像** を保存
- そのページから **関連リンク** を発見 → 未登録のものをまとめてブックマーク追加

### 事前準備（1回だけ）

この機能は **Cloudflare Worker** として動くため、まずデプロイが必要です。

```bash
cd workers/explore
pnpm install
npx wrangler login    # 初回のみ（Cloudflare アカウント）
npx wrangler deploy
```

デプロイが成功すると、`https://bookmark-explore.xxxxx.workers.dev` のような **Worker の URL** が表示されます。

### 使い方

1. アプリの一覧上部にある **探索** ボタンをクリック
2. パネル内の **設定** を開き、先ほどの Worker URL を入力 → **接続テスト** → **保存**
3. 対象を選びます（デフォルトは **未分類のみ**）
   - 未分類のみ / 全件 / 現在の絞り込み
4. **探索を開始** をクリック
5. 完了したら、結果を確認して **適用** ボタンで保存
   - タイトル・要約・タグ・サムネイルがブックマークに反映されます
6. 「発見したリンク」に未登録の関連ページがあれば、**未登録のリンクをブックマークに追加** でまとめて登録

> 注意
> - Kitesurf はベータ版のため、動画・ログイン必須サイト・ボット対策サイトでは失敗する場合があります（そのブックマークだけ失敗扱いになり、探索は続行します）
> - 対象件数が多いと時間がかかるため、まずは「未分類のみ」で少ない件数から試すのがおすすめです

---

## 7. Zed AI 連携（高度な整理）

Zed エディタの AI を使った高度なタグ付け・整理機能です。

1. 一覧上部の **AI** ボタンでパネルを開く
2. パネルの **起動** ボタンでローカル API サーバを起動（`http://localhost:9876`）
3. プロンプトテンプレート（タグ分類 / タイトル正規化 / 重複マージ提案 / コレクション分析）を **コピー**
4. Zed を開いて AI チャット（`Cmd + I`）に貼り付け、実行
5. AI が生成した JSON を確認し、パネルで **適用**

---

## 8. GitHub 同期（複数デバイス）

ブックマークを **GitHub リポジトリ経由で複数の PC 間で同期** できます。

### セットアップ

1. **GitHub の Personal Access Token（PAT）を発行**
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - スコープ **`repo`** にチェックして発行
2. **同期用リポジトリを作成**（公開・非公開どちらでも可）
3. アプリのサイドバー下部 **同期** ボタン → ダイアログに以下を入力
   - Personal Access Token / リポジトリ所有者 / リポジトリ名 / デバイス名
4. **接続テスト＆保存** → 成功するとサイドバーに緑のアイコンが表示

### 同期を実行

**同期** ボタンをクリックするだけで、以下が自動で行われます。

1. GitHub から他デバイスのデータを取得
2. ローカルとマージ
3. 自分のデバイスのデータをアップロード
4. 結果を通知（何件取り込んだ / 何件送信した）

### コンフリクト（競合）が起きたら

同じブックマークを別のデバイスでそれぞれ編集していた場合、コンフリクト解決モーダルが開きます。1件ずつ選択して解決します。

| 選択肢 | 動作 |
|---|---|
| ローカルを保持 | このデバイスの内容を残す |
| リモートを採用 | GitHub 上の内容で上書き |
| 自動マージ | タイトル・タグ・メモをいい感じに統合 |
| スキップ | 今回は保留して、後でまた同期 |

---

## 9. データの場所とバックアップ

ブックマークはローカルの SQLite データベースに保存されます。

- **macOS**: `~/Library/Application Support/bookmark-manager-pe/bookmarks.db`
- **Windows**: `%APPDATA%/bookmark-manager-pe/bookmarks.db`
- **Linux**: `~/.config/bookmark-manager-pe/bookmarks.db`

バックアップは、アプリを終了した状態で `bookmarks.db` をコピーするのが確実です。GitHub 同期を設定していれば、リポジトリ内にもデータが残るため二重に安全です。

---

## 10. よくあるトラブル

| 症状 | 対処 |
|---|---|
| アプリが起動しない | `pnpm rebuild` を実行してから `pnpm dev` |
| 同期が「接続失敗」 | トークンの期限切れ・リポジトリ名/所有者の誤りを確認 |
| AI が応答しない | `http://localhost:9876/api/health` が開くか確認、AI パネルでサーバを起動 |
| 探索がすべて失敗する | Worker URL が正しいか、`接続テスト` で確認。Kitesurf の制限（beta）の可能性 |
| 重複が検出されない | 正規化後も別 URL の場合は手動で判断 |

---

## 11. 開発者向けメモ

- `pnpm lint` — TypeScript の型チェック
- `pnpm build` — プロダクションビルド
- 詳細な実装メモは `DEV-MEMO.md` を参照